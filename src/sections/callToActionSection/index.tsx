import styles from "./styles.module.scss";
import GridPage from "@/src/grid";

export default function CallToActionSection() {
    return (
        <section className={styles.container}>
            <GridPage />
        </section>
    );
}